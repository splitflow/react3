'use client'

import { Editor } from '@/lib'
import { createEditor } from '@splitflow/editor'
import { useState } from 'react'

export default function EditorPage() {
    //const [module] = useState(() => createEditor({}))
    return 'aa'

    //return <Editor module={module} />
}
